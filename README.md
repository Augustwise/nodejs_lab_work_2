# Laboratory work #2 

[DEMO LINK](https://nodejs-lab-work-2.onrender.com/)

## Running Locally

To run this project locally, clone the repository and install dependencies with `npm install`. Then start the server with `npm start`. The application will be available at `http://localhost:3000` by default.

